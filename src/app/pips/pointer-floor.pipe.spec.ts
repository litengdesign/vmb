import { PointerFloorPipe } from './pointer-floor.pipe';

describe('PointerFloorPipe', () => {
  it('create an instance', () => {
    const pipe = new PointerFloorPipe();
    expect(pipe).toBeTruthy();
  });
});
