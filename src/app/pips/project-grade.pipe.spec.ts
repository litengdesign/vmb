import { ProjectGradePipe } from './project-grade.pipe';

describe('ProjectGradePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectGradePipe();
    expect(pipe).toBeTruthy();
  });
});
