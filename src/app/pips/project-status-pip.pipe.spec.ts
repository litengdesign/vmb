import { ProjectStatusPipPipe } from './project-status-pip.pipe';

describe('ProjectStatusPipPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectStatusPipPipe();
    expect(pipe).toBeTruthy();
  });
});
