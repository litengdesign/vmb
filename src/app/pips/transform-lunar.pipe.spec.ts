import { TransformLunarPipe } from './transform-lunar.pipe';

describe('TransformLunarPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformLunarPipe();
    expect(pipe).toBeTruthy();
  });
});
